---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: persistentvolumeautoscalers.autoscaling.gardener.cloud
spec:
  group: autoscaling.gardener.cloud
  names:
    kind: PersistentVolumeAutoscaler
    listKind: PersistentVolumeAutoscalerList
    plural: persistentvolumeautoscalers
    shortNames:
    - pva
    singular: persistentvolumeautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalPVCs
      name: PVCs
      type: integer
    - jsonPath: .status.managedPVCAs
      name: PVCAs
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PersistentVolumeAutoscaler is the Schema for the persistentvolumeautoscalers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PersistentVolumeAutoscalerSpec defines the desired state
              of PersistentVolumeAutoscaler
            properties:
              increaseBy:
                description: |-
                  IncreaseBy specifies an increase by percentage value (e.g. 10%, 20%, etc.)
                  by which the Persistent Volume Claims will be resized.
                type: string
              maxCapacity:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  MaxCapacity specifies the maximum capacity up to which PVCs are allowed
                  to be extended.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              targetRef:
                description: TargetRef specifies the reference to the workload (StatefulSet,
                  Deployment, etc.)
                properties:
                  apiVersion:
                    description: APIVersion specifies the API version of the workload
                    type: string
                  kind:
                    description: Kind specifies the kind of the workload (StatefulSet,
                      Deployment, etc.)
                    type: string
                  name:
                    description: Name specifies the name of the workload
                    type: string
                required:
                - kind
                - name
                type: object
              threshold:
                description: |-
                  Threshold specifies the threshold value in percentage (e.g. 10%, 20%, etc.)
                  Once the available capacity (free space) for the PVCs reaches or drops below
                  the specified threshold this will trigger a resize operation.
                type: string
            required:
            - targetRef
            type: object
          status:
            description: PersistentVolumeAutoscalerStatus defines the observed state
              of PersistentVolumeAutoscaler
            properties:
              conditions:
                description: Conditions specifies the status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdate:
                description: LastUpdate specifies the last time the status was updated
                format: date-time
                type: string
              managedPVCAs:
                description: ManagedPVCAs indicates the number of PVCA resources created
                  for PVCs
                type: integer
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              phase:
                description: Phase indicates the overall phase of the PVA
                type: string
              totalPVCs:
                description: TotalPVCs indicates the total number of PVCs managed
                  by this PVA
                type: integer
            required:
            - managedPVCAs
            - totalPVCs
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
